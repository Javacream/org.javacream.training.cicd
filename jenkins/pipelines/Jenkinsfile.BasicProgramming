pipeline {
    agent any
    environment {
        NAME = 'Sawitzki'
    }
    stages {
        stage('Hello') {
            environment {
                SUFFIX = '!'
            }
            steps {
                echo "Hello ${NAME} ${SUFFIX}"
                writeFile('data.txt', 'From Stage Hello')
            }
        }
        stage('Goodbye') {
            steps {
                echo "Goodbye ${NAME}"
                
            }
        }
    }
}