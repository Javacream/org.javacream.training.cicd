create public private key inside jenkins controller container
ssh-keygen

use private key to configure credential in jenkins

use public key to start JENKINS_AGENT_SSH_PUBKEY

inside docker agent change permissions on /var/run/docker.sock to 777 so that user jenkins can start containers